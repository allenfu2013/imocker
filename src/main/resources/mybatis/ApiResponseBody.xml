<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ApiResponseBody">
    <insert id="insert" parameterType="org.allen.imocker.entity.ApiResponseBody">
        insert into api_response(
            api_doc_id,
            response_key,
            response_type,
            response_required,
            response_parent,
            response_desc
        ) values (
            #{apiDocId},
            #{responseKey},
            #{responseType},
            #{responseRequired},
            #{responseParent},
            #{responseDesc}
        )
    </insert>

    <sql id="all_columns">
        id id,
        api_doc_id apiDocId,
        response_key responseKey,
        response_type responseType,
        response_required responseRequired,
        response_parent responseParent,
        response_desc responseDesc
    </sql>

    <select id="getByApiDocId" resultType="org.allen.imocker.entity.ApiResponseBody">
        select
        <include refid="all_columns"/>
        from api_response where api_doc_id = #{apiDocId}
    </select>

    <delete id="deleteByApiDocId">
        delete from api_response where api_doc_id = #{apiDocId}
    </delete>
</mapper>